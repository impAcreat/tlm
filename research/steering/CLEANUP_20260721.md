# Steering cleanup record — 2026-07-21

The cleanup was developed in the isolated lab-130 checkout and then consolidated
into the canonical `/sdc/ninghan/tlm`. Lab-50 was not modified.

The former `/sdc/ninghan/tlm-reflex-t-20260721` and
`/sdc/ninghan/tlm-steering` trees were removed after their required code,
environment, benchmark data and Qwen3.5-4B weights were moved into the canonical
tree. `research/efm` was retained as `research/efm-archive` and is inactive.

## Kept and reorganized

- Validated reusable methods: `core/{extraction,application,compiler,metrics}`.
- Model/benchmark/conditioning integration: `adapters/`.
- Artifact-only analysis and plotting: `analysis/`, `visualization/`.
- Mature 4B reference: `experiments/skill_edit_4b/`.
- Exploratory 32B Reflexion-to-T line: `experiments/reflexion_T/`.
- Historical valid run artifacts and `experiments/EXPERIMENT_LOG.md`.

The mature 4B scripts were moved from the ambiguous `latent/` directory to
`experiments/skill_edit_4b/scripts/`. Their imports and repository-root lookup
were updated for the lab-130 checkout. The 32B guide moved to
`experiments/reflexion_T/GUIDE.md` and now requires model-native causal layer
and dose calibration.

## Removed

- Model-bound legacy core modules `core/hf.py` and `core/hooks.py`; callers now
  use `adapters/models/`.
- All `latent/launch_*.sh`, cache directories, smoke logs/runs, and protocol-
  invalid raw run directories.
- Replaced 32B prototypes: `units_forward_32b.py`, `train_T_32b.py`,
  `prepare_reflex_g3_32b.py`, and `reflex_g3_eval_32b.py`.
- Replaced duplicate Reflexion scripts: `faithful_reflexion.py` and
  `summarize_faithful_reflexion.py`.
- The geometry-only `analyze_layers_32b.py` selector and the mixed-method
  `core/vectors.py` prototype.
- Early NPM/trajectory-mean-difference packages `skill_edit/` and `rollouts/`;
  their required trajectory serializer was retained with the 4B reference.
- The unvalidated cross-benchmark steering exploration under the former
  `benchmarks/` package and the negative `artifacts/skill_edit_v2/` summary.

Removed raw smoke/protocol-failure files are not recoverable from this
independent checkout. Their durable failure classification is preserved in
`runs/reflex_T_qwen3_32b_20260721/protocol_failures.md`, and scientific results
remain in `experiments/EXPERIMENT_LOG.md`.

## Verification

- Whole `research/steering` tree compiles under `envs/steering-hf`.
- `PYTHONPATH=. envs/steering-hf/bin/python -m pytest -q research/steering/tests`
  passes 7 tests.
- No current Python source imports the removed `latent`, `core.hf`,
  `core.hooks`, `skill_edit`, `rollouts`, or old `benchmarks` packages.
